apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-websocket
spec:
  selector:
    app: tic-tac-toe-websocket
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
